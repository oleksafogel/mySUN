import Link from 'next/link';

import './button.css';

const Button = () => {
  return(
    <div>
      <a href="/#sign-up" className="button">Sign Up to Waitlist</a>
    </div>
  );
}

export default Button;